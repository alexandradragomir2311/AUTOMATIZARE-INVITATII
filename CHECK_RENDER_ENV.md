# VERIFICARE RENDER ENVIRONMENT VARIABLES

Mergi pe: https://dashboard.render.com/web/automatizare-invitatii-1/env

Verifică că AI TOATE:

```
SMTP_SERVER = mail.unbr.ro
SMTP_PORT = 587
EMAIL_ADDRESS = evenimente@unbr.ro
EMAIL_PASSWORD = WsmM6$372F
SMTP_USE_TLS = true
```

**IMPORTANT:** Dacă lipsește `EMAIL_PASSWORD`, adaugă-l ACUM!

După ce adaugi, Render se redeploy automat în 1 minut.
